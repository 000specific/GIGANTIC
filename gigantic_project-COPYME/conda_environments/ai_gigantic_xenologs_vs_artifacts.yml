# GIGANTIC Xenologs Vs Artifacts Environment
# ====================================
#
# Placeholder environment for the xenologs_vs_artifacts subproject.
# Specific tool dependencies will be added as this subproject is developed.
#
# INSTALLATION:
#   From project root:
#   bash RUN-setup_environments.sh
#
#   Or manually:
#   mamba env create -f conda_environments/ai_gigantic_xenologs_vs_artifacts.yml
#
# ACTIVATION:
#   module load conda  # (HiPerGator/HPC only)
#   conda activate ai_gigantic_xenologs_vs_artifacts

name: ai_gigantic_xenologs_vs_artifacts

channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  # Core requirements
  - python>=3.9
  - nextflow>=23.0

  # Tool-specific dependencies will be added as the subproject is developed
