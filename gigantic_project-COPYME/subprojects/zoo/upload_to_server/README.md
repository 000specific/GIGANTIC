# zoo — published outputs

Server-facing data for the `zoo` holding subproject (work outside GIGANTIC
conventions — see `../README.md`). Each block surfaces through its own
subdirectory below.

| Block | Contents |
|---|---|
| `BLOCK_moroz_innovations/` | Clade "innovation" + OCL "origin" tables from a `structure_001`-only test run (6 TSVs). See that folder's README for the two-vocabulary distinction. |
| `BLOCK_leonid_requests/june_4/` | The **June 4** delivery to Leonid Moroz: OCL (orthogroups + annogroups) and secretome tables re-served with each sequence's amino-acid FASTA carried in-row (one sequence per row). See `june_4/README.md` for format and the quote-aware parsing note. |

Both blocks are one-off / test-run artifacts rather than full GIGANTIC pipeline
output — treat them with that framing.
