# AI: Claude Code | Opus 4.6 | 2026 February 28 | Purpose: OrthoHMM pipeline configuration
# Human: Eric Edsinger

# =============================================================================
# OrthoHMM Pipeline Configuration
# =============================================================================
# Edit these parameters before running the pipeline.
# These values are passed to nextflow.config via command-line overrides.

# Input proteomes directory (from genomesDB output_to_input)
proteomes_dir: "../../genomesDB/STEP_2-standardize_and_evaluate/output_to_input/gigantic_proteomes"

# OrthoHMM parameters
cpus: 8
evalue: "0.0001"
single_copy_threshold: "0.5"

# Conda environment name
conda_environment: "ai_gigantic_orthogroups"
