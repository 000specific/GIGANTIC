# AI: Claude Code | Opus 4.6 | 2026 March 03 | Purpose: TMbed pipeline configuration
# Human: Eric Edsinger

# =============================================================================
# TMbed Pipeline Configuration
# =============================================================================

# Input proteomes directory (from genomesDB output_to_input)
proteomes_dir: "../../genomesDB/output_to_input/STEP_2-standardize_and_evaluate/gigantic_proteomes"

# Number of sequences per prediction batch
batch_size: 1

# Use GPU acceleration when available
use_gpu: true

# Fall back to CPU if GPU is unavailable
cpu_fallback: true

# Conda environment name
conda_environment: "ai_gigantic_tmbed"
