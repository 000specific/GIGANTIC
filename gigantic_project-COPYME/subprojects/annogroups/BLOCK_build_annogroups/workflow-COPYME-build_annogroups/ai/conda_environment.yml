# GIGANTIC annogroups - BLOCK_build_annogroups environment
# AI: Claude Code | Opus 4.8 (1M context) | 2026 June 18
# Human: Eric Edsinger
# ====================================================================
#
# Per-BLOCK conda env scoped to BLOCK_build_annogroups (§28/§53 naming).
# Created on-demand by RUN-workflow.sh via mamba (conda fallback).
#
# Pure-Python BLOCK (no external aligners/search tools) — it parses each
# annotation source's output (one parser plugin per source) and builds the
# four canonical annogroup types. Dependencies are intentionally minimal.
#
# INSTALLATION:
#   The env is created automatically on first run via RUN-workflow.sh.
#   Manual creation:
#     mamba env create -f ai/conda_environment.yml
#
# ACTIVATION:
#   module load conda  # (HiPerGator/HPC only)
#   conda activate aiG-annogroups-build_annogroups

name: aiG-annogroups-build_annogroups

channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  # Core requirements
  - python>=3.11
  - nextflow>=23.0,<26.0  # PIN: NF 26.x strict-DSL retroactively breaks these workflows (see reference_nextflow_version_pin)

  # Configuration parsing
  - pyyaml
