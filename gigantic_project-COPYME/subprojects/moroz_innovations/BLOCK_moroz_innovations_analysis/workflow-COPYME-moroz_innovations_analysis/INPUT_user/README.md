# INPUT_user

This workflow takes all its inputs through `../START_HERE-user_config.yaml`.
There is no separate manifest file at this time -- clades and structures are
listed directly in the config.

If a future RUN grows large (many clades or structures), we may promote the
clade list to its own TSV manifest. For now, edit the config.
