# GIGANTIC sequence_groups_X_species - BLOCK_resolve_groups environment
# AI: Claude Code | Opus 4.8 (1M context) | 2026 June 18
# Human: Eric Edsinger
# ====================================================================
#
# Per-BLOCK conda env scoped to BLOCK_resolve_groups (§28/§53 naming).
# Created on-demand by RUN-workflow.sh via mamba (conda fallback).
#
# Pure-Python BLOCK (no external aligners/search tools) — it reads a producer's
# sequence-group membership and resolves it onto the species-tree clades:
# species-tree overlays (deconvolution, per-species map, composite clades).
# Dependencies are intentionally minimal.
#
# INSTALLATION:
#   The env is created automatically on first run via RUN-workflow.sh.
#   Manual creation:
#     mamba env create -f ai/conda_environment.yml
#
# ACTIVATION:
#   module load conda  # (HiPerGator/HPC only)
#   conda activate aiG-sequence_groups_X_species-resolve_groups

name: aiG-sequence_groups_X_species-resolve_groups

channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  # Core requirements
  - python>=3.11
  - nextflow>=23.0,<26.0  # PIN: NF 26.x strict-DSL retroactively breaks these workflows (see reference_nextflow_version_pin)

  # Configuration parsing
  - pyyaml
