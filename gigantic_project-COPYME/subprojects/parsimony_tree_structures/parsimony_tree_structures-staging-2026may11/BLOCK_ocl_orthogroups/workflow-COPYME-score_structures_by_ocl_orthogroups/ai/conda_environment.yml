# GIGANTIC parsimony_tree_structures - BLOCK_ocl_orthogroups environment
# AI: Claude Code | Opus 4.7 | 2026 May 11
# Human: Eric Edsinger
# ====================================================================
#
# Per-BLOCK conda env scoped to BLOCK_ocl_orthogroups.
# Created on-demand by RUN-workflow.sh via mamba (conda fallback).
#
# INSTALLATION:
#   Created automatically on first run via RUN-workflow.sh.
#   Manual creation:
#     mamba env create -f ai/conda_environment.yml
#
# ACTIVATION:
#   module load conda  # (HiPerGator/HPC only)
#   conda activate aiG-parsimony_tree_structures-ocl_orthogroups

name: aiG-parsimony_tree_structures-ocl_orthogroups

channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  # Core
  - python>=3.11
  - nextflow>=23.0,<26.0  # PIN: NF 26.x strict-DSL retroactively breaks these workflows; see HANDOFF doc

  # Configuration parsing
  - pyyaml

  # Data handling and bootstrap
  - pandas
  - numpy

  # Visualization (colorblind-safe figures)
  - matplotlib
