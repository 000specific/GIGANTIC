# Gene Coordinates Extractor Toolkit - conda env
# AI: Claude Code | Opus 4.7 | 2026 May 29
# Human: Eric Edsinger
#
# Minimal: just python (stdlib only — no third-party parsers) + nextflow.
# pyyaml is needed for RUN-workflow.sh's YAML->.params.json flatten.
#
name: aiG-research_ai-gene_coordinates_extractor
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.10
  - pyyaml
  - nextflow>=23.0,<26.0   # PIN: NF 26.x strict-DSL retroactively breaks these workflows
